- 依照AUPG/“matching model”算法：其目标不是造一张粗糙高度图，而是造一对互相关的粗糙面来组成同一条裂隙的上下壁
  - 算法采用谱合成
  - 每个波数分量有幅值（由分形维数/各向异性决定）和相位（由随机数决定）。
  - AUPG 要在波长域内按 ML/TL/MFMIN/MFMAX 规定的函数，让两张面的相位在长波上更相关、短波上更独立。
  - 故必须为“上、下两面”分别准备一套相位，并按目标相关系数去“部分相关”。
  - 做完 2D IFFT 后得到两张均值为 0 的粗糙面，再按标准差与物理尺寸缩放。